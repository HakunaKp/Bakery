export default function DescriptionDescription(description) {

    // cake is eggless and at least one other extra
    if (description) return description;
    else return "No Description Entered.";
}