export default function ShapeDescription(shape_choice) {
    if (shape_choice === "Circle") return "A circle shaped cake."
    if (shape_choice === "Rectangle") return "A square or rectangular shaped cake."
    if (shape_choice === "Heart") return "A heart shaped cake."
}